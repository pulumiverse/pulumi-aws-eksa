{
    "name": "@pulumi/aws-eksa",
    "version": "${VERSION}",
    "scripts": {
        "build": "tsc",
        "install": "node scripts/install-pulumi-plugin.js resource aws-eksa ${VERSION}"
    },
    "dependencies": {
        "@pulumi/equinix-metal": "^3.2.1"
    },
    "devDependencies": {
        "typescript": "^3.7.0"
    },
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    },
    "pulumi": {
        "resource": true
    }
}
